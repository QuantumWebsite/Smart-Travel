# Smart Travel Backend Environment Configuration

# Security
SECRET_KEY="wz_YklEpUfIdRWMXoTnBrVkIADILWTcUGN7WvLiXGW4"
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Database configuration
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=travel_deals

# CORS configuration - Add production URLs when deploying
# Can be a JSON array or comma-separated string
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:5173,http://127.0.0.1:3000,https://smart-travel.example.com

# Bright Data MCP configuration
BRIGHT_DATA_API_KEY=your_bright_data_api_key_here
BRIGHT_DATA_ZONE_USERNAME=your_zone_username_here
BRIGHT_DATA_ZONE_PASSWORD=your_zone_password_here


# JWT Settings
JWT_ALGORITHM=HS256

# Logging
LOG_LEVEL=INFO

# Application Settings
APP_NAME="Smart Travel Deals Planner API"
APP_ENVIRONMENT=development  # Change to production when deploying
GEMINI_API_KEY=your_gemini_api_key_here  # For Gemini integration

FRONTEND_URL: str = "http://localhost:3000"
EMAILS_FROM_EMAIL: str = "noreply@smarttravel.app"
EMAILS_FROM_NAME: str = "Smart Travel"
SMTP_HOST: str = "localhost"
SMTP_PORT: int = 1025 
SMTP_USER: str = "" 
SMTP_PASSWORD: str = "" 
SMTP_TLS: bool = False